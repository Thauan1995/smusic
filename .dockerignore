# Used by the deploy/docker-compose.prod.yml "caddy" service, whose build
# context is the repo root (needs both frontend/ and deploy/Caddyfile —
# see deploy/README.md). Keeps that build context small and secret-free.
.git
.claude
.local-run
docs
backend
frontend/.dart_tool
frontend/**/.dart_tool
frontend/**/build
frontend/.idea
frontend/**/*.iml
