name: auth_domain
description: >
  Auth entities, repository interface and use cases. Pure Dart - no Flutter,
  no data/presentation dependency (docs/architecture/frontend-flutter.md
  section 1.2).
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  meta: ^1.15.0
  riverpod: ^2.6.1

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
