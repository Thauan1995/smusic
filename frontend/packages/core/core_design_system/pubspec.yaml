name: core_design_system
description: >
  Shared theme, design tokens, breakpoints and reusable visual components
  (including skeleton loading states) for every presentation/* package. See
  docs/architecture/frontend-flutter.md sections 1.2, 3.4.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
