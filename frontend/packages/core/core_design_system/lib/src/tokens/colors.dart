import 'package:flutter/widgets.dart';

/// Brand color tokens. Placeholder palette for Fatia 1 - not a final visual
/// identity decision, just enough to build a coherent, theme-aware UI.
class SmusicColors {
  // Private no-op constructor, never called by design (see spacing.dart
  // for the pattern rationale).
  const SmusicColors._(); // coverage:ignore-line

  static const Color brandSeed = Color(0xFF1ED760);
  static const Color error = Color(0xFFE74C3C);
}
